import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminReportsPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Report review queue"
      description="Report triage pages will be added together with moderation actions and audit logging."
    />
  );
}
