import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminEventsPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Event moderation queue"
      description="Event moderation views will be connected once event reads and write paths are implemented."
    />
  );
}
