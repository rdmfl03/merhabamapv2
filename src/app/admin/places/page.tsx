import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminPlacesPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Place moderation queue"
      description="Place approval and rejection workflows will be implemented after the public places module exists."
    />
  );
}
