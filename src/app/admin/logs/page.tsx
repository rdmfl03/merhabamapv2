import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminLogsPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Audit log browser"
      description="Audit storage is modeled in Prisma; list and filtering UI will follow with the admin feature slice."
    />
  );
}
