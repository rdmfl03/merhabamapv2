import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminClaimsPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Claim review queue"
      description="Business claim review screens are intentionally deferred until the next feature slice."
    />
  );
}
