import { PlaceholderState } from "@/components/ui/placeholder-state";

export default function AdminPage() {
  return (
    <PlaceholderState
      eyebrow="Admin"
      title="Moderation foundation"
      description="Admin review queues, claims, reports and audit logs will be added in the next implementation steps."
    />
  );
}
