/**
 * Minimum public (visible) places required to expose a category browse page or index entry.
 * Avoids thin near-duplicate pages that only repeat a single place detail.
 */
export const MIN_PUBLIC_PLACES_FOR_CATEGORY_BROWSE = 2;
